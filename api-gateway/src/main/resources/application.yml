server:
  port: 4004

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            #REST CLIENT - http://localhost:4005/auth/login
            #API GATEWAY - http://auth-service:4005/login
            - id : auth-service-route
              uri : http://auth-service:4005
              predicates:
                - Path=/auth/**
              filters:
                - StripPrefix=1

            #REST CLIENT - http://localhost:4004/api/patients
            #API GATEWAY - http://patient-service:4000/patients
            #PATIENT SERVICE (SPRING BOOT APP) -> /patients -> List of Patients
            - id : patient-service-route
              uri : http://patient-service:4000
              predicates:
                - Path=/api/patients/**
              filters:
                - StripPrefix=1 #removes the /api from the request
                - JwtValidation #protects the patients routes, by giving access to only valid jwts


          #REST CLIENT - http://localhost:4004/api-docs/patients
          #API GATEWAY - http://paitent-service:4000/v3/api-docs
            - id: api-docs-patient-route
              uri: http://patient-service:4000
              predicates:
                - Path=/api-docs/patients
              filters:
                - RewritePath=/api-docs/patients,/v3/api-docs #replaces /api-docs/patient with /v3/api-docs in url

            - id: api-docs-auth-route
              uri: http://auth-service:4005
              predicates:
                - Path=/api-docs/auth
              filters:
                - RewritePath=/api-docs/auth,/v3/api-docs